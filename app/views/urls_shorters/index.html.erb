<p id="notice"><%= notice %></p>

<h1>Urls Shorters</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @urls_shorters.each do |urls| %>

  
  


      <tr>
      <td><%=link_to "http://#{request.host}#{(urls.url_short)}", urls%></td>
      <td><%= urls.url_long %></td>
        <td><%= link_to 'Edit', edit_urls_shorter_path(urls) %></td>
        <td><%= link_to 'Destroy', urls, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Urls Shorter', new_urls_shorter_path %>
